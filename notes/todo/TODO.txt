





------------------------------------------------------------------------------------------

- Binding Interfaces To Implementations https://laravel.com/docs/8.x/container#binding-interfaces-to-implementations

- Set `APP_DEBUG` in env based on the current project environment. Detect if running locally or live

- Enable caching

- Bootstraps are where shit should be logged

- What are queued event listeners

- Route not supported redirect. When a route method (GET|POST,etc) isn't defined for a route, then redirect them to home

- Create ENUM classes to store the validation strings 



npx babel --watch src --out-dir . --presets react-app/prod







































